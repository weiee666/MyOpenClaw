apiVersion: v1
kind: Service
metadata:
  name: openclaw-user-backend
  namespace: openclaw-user-service
  labels:
    app.kubernetes.io/name: openclaw-user-backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: openclaw-user-backend
  ports:
    - name: http
      port: 4000
      targetPort: http
